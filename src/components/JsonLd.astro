---
/**
 * Generic JSON‑LD injector
 * Usage: <JsonLd data={object} />
 */
interface Props { data: Record<string, any> }
const { data } = Astro.props;
const json = JSON.stringify(data, null, 0);
---
<script type="application/ld+json" is:inline>{json}</script>
